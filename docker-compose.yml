version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "4173:4173"
    environment:
      - VITE_API_URL=http://localhost:4000
    depends_on:
      - backend
    restart: always
